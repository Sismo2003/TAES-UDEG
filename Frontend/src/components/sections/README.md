# components/sections

Bloques de página: `Hero`, `About`, `Services`, `Contact`, etc. Una sección por archivo.

Convención: cada sección recibe sus textos como prop (o desde un store/i18n),
no los hardcodea. Para animaciones de entrada, sigue el patrón de la skill `ui`.
