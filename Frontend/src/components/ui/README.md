# components/ui

Primitivos reusables: `Button`, `Modal`, `Badge`, `Input`, etc.

Regla: una vez que un patrón se repite 2+ veces, se convierte en primitivo aquí.
Lee la skill `ui` para los contratos exactos (colores, variants, motion).
