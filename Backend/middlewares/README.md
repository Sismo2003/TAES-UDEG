# Middlewares

Aquí viven middlewares reutilizables: auth, validación, rate limiters específicos, etc.

Cuando agregues autenticación, este será el lugar para `auth.middleware.js` con
los guards `requireAuth`, `requireAdmin`, etc. (ver skill `backend`).
